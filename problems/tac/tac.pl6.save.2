#!/usr/bin/env perl6

sub MAIN (Str $file) {
  my $text = open $file;
 #  or die "Not a file ($file) unless $file.IO.f;

my @lines = reverse <$text>;
#for $line (@lines) {
  say @lines;
#}
#my $datafile= open $file;
 # for $datafile.lines -> $line {



 #say $line;
 #say $line.flip;
#}

 # my $reverse = $file.reverse;
 # say "$reverse";


#put "OK";
}
